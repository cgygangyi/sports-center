<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gym1.mapper.ItemOrderMapper">

    <insert id="addItemOrder" parameterType="com.gym1.entity.ItemOrder">
        insert into item_order (userId, itemId, venueId, commentTime, info)
        values (#{userId}, #{orderId}, #{venueId}, #{commentTime}, #{info})
    </insert>


</mapper>
