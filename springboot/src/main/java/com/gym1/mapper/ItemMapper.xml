<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gym1.mapper.ItemMapper">

    <insert id="addItem" parameterType="com.gym1.entity.Item">
        insert into comment (itemName, price, venueId, commentTime, message)
        values (#{userId}, #{orderId}, #{venueId}, #{commentTime}, #{message})
    </insert>


</mapper>
